# 端口冲突

## 已知冲突

当前配置中：

| 服务 | 端口 |
| --- | ---: |
| `wimoor-finance` | 8106 |
| `wimoor-ozon` | 8106 |

## 处理方式

1. 本地只启动其中一个服务。
2. 修改其中一个服务的 dev/prod bootstrap 端口。
3. 同步更新 Nacos 和启动脚本中的端口认知。
4. 再启动服务并确认网关路由可访问。

不要在未确认调用方和部署环境的情况下直接提交端口修改。

