<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wimoor.ozon.product.mapper.OzonListingImageMapper">

    <select id="listByDraftIdAndVariantId" resultType="com.wimoor.ozon.product.pojo.entity.OzonListingImage">
        select *
        from t_ozon_listing_image
        where draft_id = #{draftId}
        <if test="variantId == null">
            and variant_id is null
        </if>
        <if test="variantId != null">
            and variant_id = #{variantId}
        </if>
        order by sort_order asc, id asc
    </select>

    <delete id="deleteByDraftIdAndVariantId">
        delete from t_ozon_listing_image
        where draft_id = #{draftId}
        <if test="variantId == null">
            and variant_id is null
        </if>
        <if test="variantId != null">
            and variant_id = #{variantId}
        </if>
    </delete>
</mapper>
